<ul class="media-list scrollable-y" id="major-stream" height="500px">
  <% _.each(_.uniq(major.items), function(activity) { %>
  <%= partial("major-activity", {activity: activity, headless: headless}) %>
  <% }); %>
</ul>

<script>
	var stream = document.getElementById("major-stream");

	function findOffsetTop(obj) {
	  var offsetTop = 0;
	  if (obj.offsetParent) {
	    do { 
	         offsetTop += obj.offsetTop;
	       } while (obj = obj.offsetParent);
          }
          return offsetTop;
        }
	
	stream.style['height'] = '' + 
			         (window.innerHeight - findOffsetTop(stream) - 10) + 
                                 'px';
</script>
